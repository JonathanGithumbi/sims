from django.shortcuts import render

def credit_accounts(request):
    """This method will be called at the start of every term to credit accounts the core amount due for that term, 
        excluding optionals """
    pass
    